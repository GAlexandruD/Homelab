apiVersion: v1
kind: ConfigMap
metadata:
  name: quartz-nginx
data:
  default.conf: |
    server {
        listen 8080;
        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri.html $uri/ =404;
        }
    }
